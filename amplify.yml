version: 1
frontend:
  phases:
    # No build needed for static HTML/CSS/JS
    build:
      commands:
        - echo "No build required - deploying static files"
  artifacts:
    # Serve everything from the root directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
